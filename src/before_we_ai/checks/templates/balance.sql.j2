SELECT count(*) FROM "{{ journal }}"
-- ::exceptions::
SELECT {{ group_expr }} AS grp, sum({{ amount_expr }}) AS balance
FROM "{{ journal }}"
GROUP BY 1
HAVING abs(sum({{ amount_expr }})) > {{ tolerance }}
ORDER BY abs(balance) DESC, grp
